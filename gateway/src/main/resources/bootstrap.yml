spring:
  application:
    name: gateway

eureka:
  instance:
    prefer-ip-address: true
  client:
    serviceUrl:
      defaultZone: http://discovery-server:8761/eureka/

#spring:
#  application:
#    name: gateway
#  cloud:
#    config:
#      uri: http://config:8888
#      fail-fast: true
#      password: user
#      username: user